# requirements.txt

# --- 核心库 ---
# 确保 NumPy 版本至少为 1.26，以获得现代特性和性能
numpy>=1.26.0

# 确保 Pandas 版本至少为 2.1，以使用 PyArrow 后端等新功能
pandas>=2.1.0

# 确保 Scikit-learn 版本至少为 1.4，这是近期一个稳定且功能丰富的版本
scikit-learn>=1.4.0

# 确保 PyTorch 版本至少为 2.1，以获得 torch.compile 等重要的性能优化
torch>=2.1.0

# Joblib 较为稳定，1.3.0 是一个可靠的基线
joblib>=1.3.0

# --- 可选库 (仅用于 LR 范围测试) ---
# Matplotlib 3.8.0 提供了很多现代化的绘图改进
matplotlib>=3.8.0